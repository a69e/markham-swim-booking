{
  "name": "markham-swim-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "generate:vapid": "node scripts/generate-vapid-keys.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "latest",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
